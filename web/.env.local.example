# Copy to .env.local for local dev. Next.js only exposes NEXT_PUBLIC_ vars to the
# browser bundle — this one is meant to be public (it's just the API's base URL).
NEXT_PUBLIC_API_URL=http://localhost:3000
